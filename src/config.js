const defaultSettings = {
  fieldWidth: 640,
  fieldHeight: 480,
  tileSize: 16,
  player: { startX: 96, startY: 96, velocityX: 96, velocityY: 96 },
  mobs: {
    maxCount: 5
  }
};

export default defaultSettings;
